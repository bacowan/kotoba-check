{
  "name": "kotoba-check",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ts": "esbuild src/**/*.ts --bundle --outdir=dist --outbase=src",
    "build:static": "copyfiles -u 1 src/**/*.{html,css,json,png} dist",
    "build": "npm-run-all build:ts build:static"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "esbuild": "^0.25.5",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3"
  }
}
